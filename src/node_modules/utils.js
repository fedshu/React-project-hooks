import {parse} from 'query-string'; // library which parse path of the page

export const range = (start, end) => {
   return [...Array(end).keys()].map(el => el + start);
}

export const limit = 10;

export const getPaginator = search => {
    //parsedSearch - object
    const parsedSearch = parse(search);
    // if page, add number page else add 1 page
    const currentPage = parsedSearch.page ? Number(parsedSearch.page) : 1;
    const offset = currentPage * 10 - limit;
    return {currentPage, offset};
}

// offset = 2 * 10 - 10 = 20
// on the 1 page offset = 0
// on the 2 page offset = 10
// on the 3 page offset = 20
